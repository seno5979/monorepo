# CI/CD for test-golden-path-svc
# Auto-generated by IDP Golden Path - Uses shared reusable workflows
# Team: platform
name: test-golden-path-svc

on:
  pull_request:
    paths:
      - 'services/test-golden-path-svc/**'
      - '.github/workflows/test-golden-path-svc-ci.yml'
  push:
    branches: [main]
    paths:
      - 'services/test-golden-path-svc/**'
      - '.github/workflows/test-golden-path-svc-ci.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write
  pull-requests: write

jobs:
  build-test:
    uses: ./.github/workflows/shared-build-test.yml
    with:
      service_name: test-golden-path-svc
      service_path: services/test-golden-path-svc
      runtime: KOTLIN_GRADLE
      use_sonarqube: true
      use_snyk: true
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

  docker-build:
    needs: build-test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    uses: ./.github/workflows/shared-docker-build.yml
    with:
      service_name: test-golden-path-svc
      service_path: services/test-golden-path-svc
      team_name: platform
    secrets:
      AWS_ECR_ROLE_ARN: ${{ secrets.AWS_ECR_ROLE_ARN }}

  deploy-dev:
    needs: docker-build
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/shared-helm-deploy.yml
    with:
      service_name: test-golden-path-svc
      service_path: services/test-golden-path-svc
      team_name: platform
      environment: dev
      image_tag: ${{ github.sha }}
    secrets:
      AWS_DEPLOY_ROLE_ARN: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}

  deploy-uat:
    needs: deploy-dev
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/shared-helm-deploy.yml
    with:
      service_name: test-golden-path-svc
      service_path: services/test-golden-path-svc
      team_name: platform
      environment: uat
      image_tag: ${{ github.sha }}
    secrets:
      AWS_DEPLOY_ROLE_ARN: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}

  deploy-prod:
    needs: deploy-uat
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/shared-helm-deploy.yml
    with:
      service_name: test-golden-path-svc
      service_path: services/test-golden-path-svc
      team_name: platform
      environment: prod
      image_tag: ${{ github.sha }}
    secrets:
      AWS_DEPLOY_ROLE_ARN: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
